const UserController = require("./user.controller");
const authController = require("./auth.controller");
const blogController = require("./discussion.controller")
module.exports = {
    UserController,
    authController,
    blogController
}